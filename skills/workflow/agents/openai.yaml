interface:
  display_name: "Workflow"
  short_description: "One-case classical TDD from an agreed plan and TODO list"
  default_prompt: "Use $workflow to start from an existing TODO in the agreed plan and execute one test case at a time with review gates. Reuse the agreed plan and TODO list; revisit them only if a new decision changes the approved behavior or scope."

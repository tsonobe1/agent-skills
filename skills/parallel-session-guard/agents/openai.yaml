interface:
  display_name: "Parallel Session Guard"
  short_description: "Inspect active Codex work before starting parallel sessions."
  default_prompt: "Use $parallel-session-guard to inspect live Codex task status and Git worktree evidence, identify ownership, and recommend a conflict-safe work area or the owning task's follow-up queue."
